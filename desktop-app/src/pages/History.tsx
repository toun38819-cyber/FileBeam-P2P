import dayjs from 'dayjs';
import relativeTime from 'dayjs/plugin/relativeTime';
import { useTransferStore } from '@/stores/transferStore';
dayjs.extend(relativeTime);
export default function History(): JSX.Element { const { history } = useTransferStore(); return <div className="space-y-4"><div className="flex flex-wrap items-center gap-3 rounded-3xl border border-white/10 bg-white/5 p-4 text-white"><input placeholder="🔍 Search files..." className="min-w-[260px] flex-1 rounded-2xl border border-white/10 bg-white/5 px-4 py-3 outline-none" /><button className="rounded-2xl bg-white/10 px-4 py-3">All ▼</button><button className="rounded-2xl bg-white/10 px-4 py-3">Any Device ▼</button><button className="rounded-2xl bg-white/10 px-4 py-3">This Week ▼</button><button className="rounded-2xl bg-white/10 px-4 py-3">Export CSV</button></div>{history.length ? <div className="rounded-3xl border border-white/10 bg-white/5 p-4 text-white shadow-glass"><div className="mb-4 text-white/50">Today</div><div className="space-y-3">{history.map((entry) => <div key={entry.id} className="flex items-center justify-between rounded-2xl border border-white/5 px-4 py-3"><div><div className="font-medium">{entry.direction === 'send' ? '↑' : '↓'} {entry.filename}</div><div className="text-sm text-white/50">{entry.peerName} • {dayjs(entry.createdAt).fromNow()}</div></div><div className="font-mono text-sm text-success">{entry.speedMBps.toFixed(1)} MB/s</div></div>)}</div></div> : <div className="grid min-h-[50vh] place-items-center rounded-3xl border border-white/10 bg-white/5 text-center text-white shadow-glass"><div><div className="text-2xl font-semibold">No transfers yet</div><div className="mt-2 text-white/60">Connect a device and send your first file.</div></div></div>}</div>; }
