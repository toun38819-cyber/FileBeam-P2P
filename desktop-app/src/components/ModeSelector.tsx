interface Props { value: 'local' | 'webrtc'; onChange(value: 'local' | 'webrtc'): void; }
export function ModeSelector({ value, onChange }: Props): JSX.Element { return <div className="inline-flex rounded-2xl border border-white/10 bg-white/5 p-1 text-sm text-white"><button className={`rounded-xl px-4 py-2 ${value === 'local' ? 'bg-success/15 text-success' : 'text-white/60'}`} onClick={() => onChange('local')}>🟢 Local (Fast)</button><button className={`rounded-xl px-4 py-2 ${value === 'webrtc' ? 'bg-accent/15 text-accent' : 'text-white/60'}`} onClick={() => onChange('webrtc')}>🔵 Internet P2P</button></div>; }
