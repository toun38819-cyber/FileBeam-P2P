import { Area, AreaChart, ResponsiveContainer } from 'recharts';
interface Props { data: number[]; }
export function SpeedGraph({ data }: Props): JSX.Element { const chartData = data.map((value, index) => ({ index, speed: value })); const current = data[data.length - 1] ?? 0; const peak = Math.max(0, ...data); return <div className="rounded-3xl border border-white/10 bg-white/5 p-5 text-white shadow-glass"><div className="font-mono text-3xl text-success">{current.toFixed(1)} MB/s</div><div className="mb-3 text-sm text-white/50">Peak: {peak.toFixed(1)} MB/s</div><div className="h-32 w-full"><ResponsiveContainer><AreaChart data={chartData}><defs><linearGradient id="speedFill" x1="0" y1="0" x2="0" y2="1"><stop offset="0%" stopColor="#6C63FF" stopOpacity={0.35} /><stop offset="100%" stopColor="#43CFFF" stopOpacity={0.02} /></linearGradient></defs><Area type="monotone" dataKey="speed" stroke="#43CFFF" fill="url(#speedFill)" strokeWidth={2} /></AreaChart></ResponsiveContainer></div></div>; }
