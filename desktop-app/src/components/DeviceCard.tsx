import type { DeviceInfo } from '@/types';
interface Props { device: DeviceInfo; connectionType: 'local' | 'webrtc'; ping: number; speed: number; }
export function DeviceCard({ device, connectionType, ping, speed }: Props): JSX.Element { return <div className="rounded-3xl border border-white/10 bg-white/5 p-5 text-white shadow-glass"><div className="flex items-start justify-between"><div><div className="text-lg font-semibold">{device.device_name}</div><div className="text-sm text-white/60">{device.device_type}</div></div><div className={`rounded-full px-3 py-1 text-xs ${connectionType === 'local' ? 'bg-success/15 text-success' : 'bg-accent/15 text-accent'}`}>{connectionType === 'local' ? '🟢 Local WiFi' : '🔵 Internet P2P'}</div></div><div className="mt-4 grid grid-cols-3 gap-3 text-sm"><div><div className="text-white/50">IP</div><div className="font-mono">{device.ip}</div></div><div><div className="text-white/50">Ping</div><div className="font-mono">{ping}ms</div></div><div><div className="text-white/50">Speed</div><div className="font-mono text-success">{speed.toFixed(1)} MB/s</div></div></div></div>; }
