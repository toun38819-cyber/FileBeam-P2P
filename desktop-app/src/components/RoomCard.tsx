import type { RoomData } from '@/types';
interface Props { room: RoomData; connectedPeer?: string; onCopy(): void; onShare(): void; onDisconnect(): void; }
export function RoomCard({ room, connectedPeer, onCopy, onShare, onDisconnect }: Props): JSX.Element { const joined = Boolean(connectedPeer); return <div className="rounded-3xl border border-white/10 bg-white/5 p-5 text-white shadow-glass"><div className="text-sm text-white/60">{joined ? '🔗 Connected to Room' : '📡 Hosting Room'}</div><div className="mt-3 font-mono text-2xl font-semibold">{room.room_id}</div><div className="mt-1 flex items-center gap-3 text-sm text-white/60"><span>{room.room_code}</span><button onClick={onCopy}>Copy</button></div><div className="mt-4 text-sm text-success">{joined ? `✓ ${connectedPeer} connected` : 'Waiting for device…'}</div><div className="mt-4 flex gap-2"><button className="rounded-xl bg-white/10 px-3 py-2" onClick={onShare}>Share</button><button className="rounded-xl bg-danger/20 px-3 py-2 text-danger" onClick={onDisconnect}>Disconnect</button></div></div>; }
