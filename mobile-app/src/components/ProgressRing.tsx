import React from 'react';
import Svg, { Circle, Text as SvgText, Defs, LinearGradient, Stop } from 'react-native-svg';
interface Props { progress: number; }
export function ProgressRing({ progress }: Props): JSX.Element { const radius = 82; const circumference = 2 * Math.PI * radius; const offset = circumference - (progress / 100) * circumference; return <Svg width={220} height={220}><Defs><LinearGradient id="grad" x1="0" y1="0" x2="1" y2="1"><Stop offset="0%" stopColor="#6C63FF" /><Stop offset="100%" stopColor="#43CFFF" /></LinearGradient></Defs><Circle cx="110" cy="110" r={radius} stroke="rgba(255,255,255,0.08)" strokeWidth="12" fill="none" /><Circle cx="110" cy="110" r={radius} stroke="url(#grad)" strokeWidth="12" fill="none" strokeDasharray={circumference} strokeDashoffset={offset} strokeLinecap="round" rotation="-90" origin="110,110" /><SvgText x="110" y="118" fill="#fff" fontSize="28" fontWeight="700" textAnchor="middle">{Math.round(progress)}%</SvgText></Svg>; }
